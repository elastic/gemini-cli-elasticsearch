prompt = """
Please search for `{{args}}` in the Elasticsearch cluster using the platform.core.search tool from the elastic-agent-builder MCP server.

This tool accepts an Elasticsearch DSL query.
"""