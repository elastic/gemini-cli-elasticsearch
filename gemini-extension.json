{
  "name": "gemini-cli-elastic-agent-builder",
  "version": "0.1.0",
  "mcpServers": {
    "elastic-agent-builder": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "${KIBANA_URL}/api/agent_builder/mcp",
        "--header",
        "Authorization: ApiKey ${API_KEY}"
      ]
    }
  }
}
